import React, { useContext, useState} from 'react';
import { Post } from './Post';
import { FavoriteContext } from './Context';

const Favorites = () => {
  // const {isFavorite, Id, setIsFavorite}=useContext(FavoriteContext)

 
 
  return (
   
    <div>

    </div>

  );
};

export default Favorites;